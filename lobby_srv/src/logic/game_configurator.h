#ifndef __TR_GAME_CONFIGURATOR__
#define __TR_GAME_CONFIGURATOR__

namespace game {

	class configurator
	{
	public:
		configurator();
		~configurator();

		static configurator& instance();


	};
}

#endif
